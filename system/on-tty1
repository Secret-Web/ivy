python3.6 ivy/system/on-boot.py

if [ -f ivy/data/config.json ]; then
    if [[ -z "$DISPLAY" ]] && [[ $(tty) = /dev/tty1 ]]; then
        startx
        exit 0
    fi
else
    cd ivy
    python3.6 install.py
    exit 0
fi

exit 1
